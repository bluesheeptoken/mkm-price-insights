{
  "name": "mkm-price-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:userscript": "esbuild src/content.ts --bundle --format=iife --target=es2019 --outfile=script-build/mkm-trends.user.js",
    "build:extension": "esbuild src/content.ts --bundle --outfile=dist/content.js && cp src/manifest.json dist",
    "watch": "esbuild src/content.ts --bundle --outfile=dist/content.js --watch=forever",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "dev": "concurrently \"npm:watch\" \"npx live-server .\" "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "@types/jest": "^30.0.0",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
